numbers = [1, 2, 3, 4, 5, 6]
print (len(numbers))

"""
.append = add an object to the end of the index
.insert = add an object to a specified index`
.remove = deletes the specified items in the list
.clear = clears the entire list
len = returns the number of elements in the list
"""
