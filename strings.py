course = 'python for noobs'

#.upper = Converts lower case letter to all upper case
#.lower = Converts upper case letters to all lower case
print(course.upper())

#.find = Finds the first occurence of the specified value
print (course.find('p'))

#.replace = Replace some characters inside the specified value
print(course.replace('for', '4'))

#in = Returns True if a sequence with the specified value is present in the object
print ('python' in course)

x5 = "robot"

print (x5)